#include <stdio.h>

int main() {
    int ch,a,b;
    scanf("%d %d %d",&ch,&a,&b);
    if(ch==1)
    {
    switch(ch)
    {
    case 1:
        printf("Sum =%d",a+b);
        break;
        }
    }

    return 0;
}
